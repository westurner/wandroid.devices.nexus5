============
Installation
============

At the command line::

    $ easy_install wandroid.devices.nexus5

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wandroid.devices.nexus5
    $ pip install wandroid.devices.nexus5